const initialState = {
  personaltasks: [],
  bookmarks: [],
  recommended: [],
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
